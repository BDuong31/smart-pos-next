import NftsView from "@/sections/admin-dashboard/view/admin-ntfs-view";

export const metadata = {
    title: 'Nfts | Baso Spark',
}

export default function NftsPage() {
    return(
        <NftsView/>
    );
}