const ROOTS = {
    HOME: '/',
    DASHBOARD: '/dashboard',

}

export const paths = {
    // xác thực
    auth: {
        login: `${ROOTS.HOME}/login`,
        register: `${ROOTS.HOME}/register`,
    },
    settings: `${ROOTS.HOME}/settings`,
}